import { PreviewsSortByDatePipe } from './previews-sort-by-date.pipe';

describe('PreviewsSortByDatePipe', () => {
  it('create an instance', () => {
    const pipe = new PreviewsSortByDatePipe();
    expect(pipe).toBeTruthy();
  });
});
